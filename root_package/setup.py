from setuptools import setup, find_packages

setup(
    name='linkage',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
        'matplotlib',
        'numpy',
        'sympy',
        # Add more dependencies as needed
    ],
)
